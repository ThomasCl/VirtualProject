/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "www.google.com", "www.ikea.com", "www.u-buy.be"],
  },
  reactStrictMode: false,
};

module.exports = nextConfig;
